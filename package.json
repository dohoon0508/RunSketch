{
  "name": "running-loop-generator",
  "version": "1.0.0",
  "description": "목표 거리에 맞춰 자동으로 루프 런닝 코스를 생성하는 앱",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && pip install -r requirements.txt && cd ../frontend && npm install"
  },
  "keywords": ["running", "route", "kakao", "maps", "gpx"],
  "author": "Running App",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
